<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Be My Valentine ❤️</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
  * {
    box-sizing: border-box;
  }

  body {
    margin: 0;
    min-height: 100vh;
    background: linear-gradient(135deg, #ff9a9e, #fad0c4);
    font-family: 'Arial', sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    overflow-x: hidden;
  }

  .card {
    background: #fff;
    border-radius: 20px;
    padding: 24px;
    width: 92%;
    max-width: 360px;
    text-align: center;
    box-shadow: 0 15px 35px rgba(0,0,0,0.2);
    position: relative;
  }

  h1 {
    color: #ff4d6d;
    margin-bottom: 10px;
  }

  p {
    color: #444;
    font-size: 16px;
    line-height: 1.4;
  }

  iframe {
    width: 100%;
    height: 200px;
    border: none;
    border-radius: 15px;
    margin: 15px 0;
  }

  .distance {
    font-weight: bold;
    color: #ff4d6d;
    margin-bottom: 15px;
  }

  .buttons {
    position: relative;
    height: 70px;
    margin-top: 10px;
  }

  button {
    padding: 12px 24px;
    border-radius: 30px;
    border: none;
    font-size: 16px;
    cursor: pointer;
    position: absolute;
  }

  #yesBtn {
    background: #ff4d6d;
    color: white;
    left: 30px;
  }

  #noBtn {
    background: #ccc;
    right: 30px;
  }

  .hidden {
    display: none;
  }

  .final {
    font-size: 18px;
    color: #ff4d6d;
  }

  /* HEART CONFETTI */
  .heart {
    position: fixed;
    bottom: -20px;
    font-size: 20px;
    animation: floatUp 4s linear forwards;
    color: #ff4d6d;
    pointer-events: none;
  }

  @keyframes floatUp {
    0% {
      transform: translateY(0) scale(1);
      opacity: 1;
    }
    100% {
      transform: translateY(-120vh) scale(1.5);
      opacity: 0;
    }
  }
</style>
</head>

<body>

<div class="card">
  <h1>Herro my sweet love ❤️‍🔥</h1>

  <p>
    Even with miles between us,<br>
    my heart is always with you.
  </p>

  <!-- MAP -->
  <iframe
    src="https://www.google.com/maps/embed?pb=!1m28!1m12!1m3!1d7601718.132126781!2d-111.349941!3d31.219123!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!4m13!3e0!4m5!1s0x80c7396f1a1b45c3%3A0x7b90b2a2b1766fa1!2sDeath%20Valley%2C%20CA!3m2!1d36.505389!2d-117.079407!4m5!1s0x865c58af07df1e13%3A0xaed7a1df94f9df84!2sSan%20Antonio%2C%20TX!3m2!1d29.4241219!2d-98.49362819999999"
    loading="lazy">
  </iframe>

  <div class="distance" id="distance"></div>

  <p><strong>Will you please be my cutie widdo valentine? 💖</strong></p>

  <div class="buttons">
    <button id="yesBtn"> absolutely i do!❤️‍🔥</button>
    <button id="noBtn">NO isss dont be liking dee ☹️</button>.  
  </div>

  <div id="finalMessage" class="hidden">
    <p class="final">
      YAY!!! 💘💘💘<br><br>
      No distance could ever stop us.<br>
      I choose you — today and always ❤️ to the moon and back🌙
    </p>
  </div>
</div>

<script>
  // Distance calculation
  const deathValley = { lat: 36.505389, lon: -117.079407 };
  const sanAntonio = { lat: 29.424122, lon: -98.493628 };

  function getDistance(lat1, lon1, lat2, lon2) {
    const R = 6371; // km
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLon = (lon2 - lon1) * Math.PI / 180;
    const a =
      Math.sin(dLat/2) * Math.sin(dLat/2) +
      Math.cos(lat1 * Math.PI / 180) *
      Math.cos(lat2 * Math.PI / 180) *
      Math.sin(dLon/2) * Math.sin(dLon/2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1-a));
    return R * c;
  }

  const km = getDistance(
    deathValley.lat, deathValley.lon,
    sanAntonio.lat, sanAntonio.lon
  );

  const miles = (km * 0.621371).toFixed(0);
  document.getElementById("distance").innerText =
    `📍 ${miles} miles apart — still choosing you`;

  // No button runs away
  const noBtn = document.getElementById("noBtn");
  noBtn.addEventListener("mouseover", () => {
    const x = Math.random() * 200 - 100;
    const y = Math.random() * 200 - 100;
    noBtn.style.transform = `translate(${x}px, ${y}px)`;
  });

  // Yes button
  const yesBtn = document.getElementById("yesBtn");
  yesBtn.addEventListener("click", () => {
    document.querySelector(".buttons").style.display = "none";
    document.getElementById("finalMessage").classList.remove("hidden");
    launchHearts();
  });

  // Heart confetti
  function launchHearts() {
    for (let i = 0; i < 30; i++) {
      const heart = document.createElement("div");
      heart.classList.add("heart");
      heart.innerText = "💖";
      heart.style.left = Math.random() * 100 + "vw";
      heart.style.animationDuration = (Math.random() * 2 + 3) + "s";
      document.body.appendChild(heart);

      setTimeout(() => {
        heart.remove();
      }, 4000);
    }
  }
</script>

</body>
</html>
